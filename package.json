{
  "name": "cue",
  "version": "0.5.0",
  "license": "MIT",
  "bin": {
    "cue": "./src/cli.ts"
  },
  "files": [
    "src",
    "tsconfig.json",
    "prompts",
    "ui/build/client"
  ],
  "type": "module",
  "scripts": {
    "cue": "bun run src/cli.ts",
    "test": "bun test",
    "ui:dev": "bun run --cwd ui dev",
    "ui:build": "bun run --cwd ui build",
    "ui:check": "bun run --cwd ui typecheck",
    "ui:lint": "bun run --cwd ui lint",
    "ui:format": "bun run --cwd ui format",
    "fixtures": "bun run scripts/fixtures.ts",
    "lint": "oxlint --disable-nested-config",
    "format": "oxfmt --disable-nested-config",
    "format:check": "oxfmt --disable-nested-config --check",
    "check": "bun run lint && bun run format:check && tsc --noEmit && bun test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "prepare": "husky"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "consola": "^3.4.2",
    "ora": "^9.4.1",
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "mermaid": "^11.17.0",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "oxlint-tsgolint": "^7.0.2001",
    "vitepress": "^2.0.0-alpha.19",
    "vitepress-plugin-mermaid": "^2.0.17"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "oxlint --fix"
    ]
  }
}
