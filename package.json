{
  "name": "conductor",
  "version": "0.2.0",
  "bin": {
    "conductor": "./src/cli.ts"
  },
  "files": [
    "src",
    "prompts"
  ],
  "type": "module",
  "scripts": {
    "conductor": "bun run src/cli.ts",
    "test": "bun test",
    "lint": "oxlint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "bun run lint && bun run format:check && bunx tsc --noEmit && bun test"
  },
  "dependencies": {
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0"
  }
}
